[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["."]
addopts = "-v --tb=short --cov=backend --cov-report=term-missing"
asyncio_mode = "auto"
filterwarnings = ["ignore::pytest.PytestUnraisableExceptionWarning", "ignore::DeprecationWarning:pytest_asyncio"]
